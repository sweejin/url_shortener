<h3>This is the urls/new.html.erb file</h3>
<%= render 'form' %>
<%= link_to 'Back', urls_path %>

<!-- Note that because _form.html.erb is a partial, we can use the render method to display the form as part of new.html.erb. -->

<%= link_to 'Shorten a URL!', new_url_path %> 
